{
  "name": "gatsby-starter-builder",
  "version": "1.0.0",
  "description": "Example Gatsby, and Builder.io project",
  "dependencies": {
    "@builder.io/gatsby": "^3.0.1",
    "@builder.io/react": "^1.1.47",
    "@builder.io/widgets": "^1.2.21",
    "gatsby": "^4.0.2",
    "react": "^17.0.1",
    "react-dom": "^17.0.2",
    "react-helmet": "^6.1.0"
  },
  "keywords": [
    "gatsby",
    "Builder.io",
    "Gatsby starter template"
  ],
  "license": "MIT",
  "main": "n/a",
  "scripts": {
    "clean": "gatsby clean",
    "start": "npm run develop",
    "build": "npm run clean && gatsby build",
    "develop": "npm run clean && gatsby develop",
    "deploy": "npm run clean && gatsby build --prefix-paths && gh-pages -d public",
    "format": "prettier --write  \"./**/*.js{x,}\""
  },
  "devDependencies": {
    "gh-pages": "^2.2.0",
    "prettier": "^1.15.3"
  },
  "resolutions": {
    "react-scripts": "^3.4.1"
  }
}
